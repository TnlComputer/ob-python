import logging

logging.basicConfig(level=logging.INFO)
logging.info("Starting Info")
logging.warning("Starting Info")
logging.debug("Starting Debug")
logging.critical("Starting Critical")
logging.error("Starting Error")
logging.fatal("Starting Fatal")
